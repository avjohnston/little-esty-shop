<%= render partial: 'form', locals: { header_2: "Editing #{@item.name}",
                                      path: merchant_item_path(@merchant, @item),
                                      method: :patch,
                                      button_text: "Update"
                                    } %>
